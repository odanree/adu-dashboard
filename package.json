{
  "name": "adu-dashboard",
  "version": "2.0.0",
  "description": "ADU Construction Progress Dashboard - workspace root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@adu-dashboard/ui",
    "build": "npm run build --workspace=@adu-dashboard/ui",
    "preview": "npm run preview --workspace=@adu-dashboard/ui",
    "type-check": "npm run type-check --workspace=@adu-dashboard/ui",
    "lint": "biome lint packages",
    "format": "biome format --write packages",
    "check": "biome check packages",
    "test": "cd tests && npm install && npm test",
    "test:e2e": "cd tests && npm install && npm run test:e2e",
    "test:all": "cd tests && npm install && npm run test:all",
    "test:watch": "cd tests && npm install && npm test -- --watch"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  },
  "engines": {
    "node": "20.x"
  }
}
