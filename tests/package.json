{
  "name": "adu-dashboard-tests",
  "version": "1.0.0",
  "description": "ADU Dashboard test suite",
  "scripts": {
    "test": "jest",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0",
    "node-fetch": "^3.3.2"
  }
}

